<header class="site-header">
    <nav class="menu">
        <a href="/es">
            <div class="element-menu">
                <svg viewBox="0 0 24 24" aria-hidden="true">
                    <path
                        d="M3 10.5L12 3l9 7.5V21a1 1 0 0 1-1 1h-5v-6H9v6H4a1 1 0 0 1-1-1z"
                    ></path>
                </svg>
            </div>
        </a>

        <a href="/es/libros">
            <div class="element-menu">Jlurbook</div>
        </a>

        <a href="/es/ayri">
            <div class="element-menu">Ayri</div>
        </a>

        <a href="/es/galeria">
            <div class="element-menu">Dibujos</div>
        </a>

        <a href="/es/juegos">
            <div class="element-menu">Juegos</div>
        </a>

        <a href="/es/contacto">
            <div class="element-menu">
                <svg viewBox="0 0 24 24" aria-hidden="true">
                    <path d="M3 5h18v14H3z"></path>
                    <path d="M3 5l9 7 9-7"></path>
                </svg>
            </div>
        </a>

        <a href="/val" class="lang-btn" aria-label="Valencià">
            <img src="/img/val.jpg" />
        </a>

        <a href="/es" class="lang-btn" aria-label="Español">
            <img src="/img/es.jpg" />
        </a>
    </nav>
</header>

<style>
    .site-header {
        padding: 0;
        margin: 0;
        background-color: black;
        color: white;
        width: 100%;
    }

    .menu {
        display: flex;
        gap: 0.3rem;
        overflow-x: auto; /* 👈 scroll horizontal */
        white-space: nowrap;
        padding: 0.5rem;
    }

    .menu::-webkit-scrollbar {
        display: none; /* opcional, más limpio */
    }

    .menu a {
        position: relative;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        padding: 0.8rem;
        border-radius: 10px;
        background-color: rgba(255, 255, 255, 0.05);
        color: white;
        transition: all 0.3s ease;
    }

    .menu svg {
        width: 1.5rem; /* más grandes */
        height: 2rem;
        fill: none;
        stroke: currentColor;
        stroke-width: 2;
        stroke-linecap: round;
        stroke-linejoin: round;
        transition: transform 0.3s ease;
    }

    .menu a:hover svg {
        transform: translateY(-2px) scale(1.2);
    }

    .menu a:hover {
        background-color: var(
            --accent,
            #ff6f61
        ); /* fallback color si no hay var */
        color: #fff;
        transform: translateY(-3px) scale(1.1);
        box-shadow: 0 8px 20px rgba(0, 0, 0, 0.3);
    }

    .menu a::after {
        content: "";
        position: absolute;
        left: 10%;
        bottom: 5px;
        width: 80%;
        height: 3px;
        background-color: white;
        border-radius: 2px;
        opacity: 0;
        transition: all 0.3s ease;
    }

    .menu a:hover::after {
        opacity: 1;
        background-color: #fff;
    }

    .element-menu {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
    }

    .lang-btn {
        flex: 0 0 auto;
        width: 35px;
        height: 35px;
        border-radius: 50%;
        overflow: hidden;
        cursor: pointer;
        transition: transform 0.2s ease;
    }

    .lang-btn:hover {
        transform: scale(1.2);
    }

    .lang-btn img {
        width: 100%;
        height: 100%;
        object-fit: cover;
    }
</style>
